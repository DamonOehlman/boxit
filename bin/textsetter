#!/usr/bin/env node

var TextSetter = require('../lib/textsetter'),
    setter = new TextSetter(process.cwd()),
    out = require('out');
    
setter.on('processor', function(processor) {
    processor.run(function(err) {
        if (err) {
            out('!{red}{0}', err);
        }
    });
});